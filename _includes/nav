{% for node in site.navigation %}


  {% if page.url == node.url %}
    <a href="{{ BASE_PATH }}{{ node.url | pure_url }}" class="active item">{{node.title}}</a>
  {% else %}
    <a href="{{ BASE_PATH }}{{ node.url | pure_url }}" class="item">{{node.title}}</a>
  {% endif %}


{% endfor %}